diff --git a/node_modules/app-builder-lib/templates/msi/template.xml b/node_modules/app-builder-lib/templates/msi/template.xml
index 2d5cd3c..928bf55 100644
--- a/node_modules/app-builder-lib/templates/msi/template.xml
+++ b/node_modules/app-builder-lib/templates/msi/template.xml
@@ -16,6 +16,33 @@
     <MajorUpgrade AllowSameVersionUpgrades="yes" DowngradeErrorMessage='A newer version of "[ProductName]" is already installed.'/>
     <MediaTemplate CompressionLevel="${compressionLevel}" EmbedCab="yes"/>
 
+    <!-- MemoryLane (patches/app-builder-lib): fleet-upgrade resilience.
+         killAppProcesses: helpers surviving from old versions hold handles in
+         the install dir; without this kill before files-in-use validation, a
+         silent upgrade defers file replacement to reboot (3010) and leaves a
+         half-applied install. Runs on install, upgrade, repair and uninstall.
+         The path-length guard keeps a hypothetically unresolved
+         APPLICATIONFOLDER from matching every process on the machine.
+         registerLaunchTask: starts the app once after install/upgrade/repair
+         via a trigger-less scheduled task (assets/msi-launch-task.ps1) — a
+         SYSTEM custom action cannot spawn into the user session directly. The
+         task never fires on its own; rollback and uninstall delete it so no
+         orphan is left behind. -->
+    <Property Id="KillAppPs" Value="powershell.exe"/>
+    <CustomAction Id="killAppProcesses" Property="KillAppPs" ExeCommand='-NoProfile -ExecutionPolicy Bypass -Command "$d = &apos;[APPLICATIONFOLDER]&apos;; if ($d.Length -gt 3) { Get-Process -ErrorAction SilentlyContinue | Where-Object { $_.Path -like ($d + &apos;*&apos;) } | Stop-Process -Force -ErrorAction SilentlyContinue }"' Execute="immediate" Return="ignore"/>
+    <Property Id="LaunchTaskPs" Value="powershell.exe"/>
+    <CustomAction Id="registerLaunchTask" Property="LaunchTaskPs" ExeCommand='-NoProfile -ExecutionPolicy Bypass -File "[APPLICATIONFOLDER]resources\assets\msi-launch-task.ps1" -Register' Execute="deferred" Impersonate="no" Return="ignore"/>
+    <Property Id="RollbackLaunchSchtasks" Value="schtasks.exe"/>
+    <CustomAction Id="rollbackLaunchTask" Property="RollbackLaunchSchtasks" ExeCommand='/Delete /F /TN "${productName} Launcher"' Execute="rollback" Impersonate="no" Return="ignore"/>
+    <Property Id="DeleteLaunchSchtasks" Value="schtasks.exe"/>
+    <CustomAction Id="deleteLaunchTask" Property="DeleteLaunchSchtasks" ExeCommand='/Delete /F /TN "${productName} Launcher"' Execute="deferred" Impersonate="no" Return="ignore"/>
+    <InstallExecuteSequence>
+      <Custom Action="killAppProcesses" Before="InstallValidate">1</Custom>
+      <Custom Action="deleteLaunchTask" After="InstallInitialize">REMOVE~="ALL" AND NOT UPGRADINGPRODUCTCODE</Custom>
+      <Custom Action="rollbackLaunchTask" Before="registerLaunchTask">NOT (REMOVE~="ALL")</Custom>
+      <Custom Action="registerLaunchTask" Before="InstallFinalize">NOT (REMOVE~="ALL")</Custom>
+    </InstallExecuteSequence>
+
     <Property Id="WIXUI_INSTALLDIR" Value="APPLICATIONFOLDER"/>
     <Property Id="ApplicationFolderName" Value="${installationDirectoryWixName}"/>
     <Property Id="WixAppFolder" Value="WixPerUserFolder"/>
