# archive

Retired plugin material kept for reference. Nothing here is auto-loaded by Claude
Code (skills only load from `plugins/memorylane/skills/`).

- **skills/time-report/** - the old time-report skill. Removed from the active
  plugin to cut router overlap. The app still ships an equivalent `time_report`
  MCP prompt (`src/main/mcp/prompts.ts`), so the capability is not lost. Kept here,
  versioned and shareable, in case the richer skill version is wanted again.
